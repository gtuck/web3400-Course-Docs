# macOS
.DS_Store

# AI assistant instructions and agents are TRACKED in git:
# AGENTS.md is canonical; CLAUDE.md, CURSOR.md, GEMINI.md, WINDSURF.md and the
# dot-directory copies are symlinks to it. .agents/ holds the canonical agents.

# Local-only assistant state (machine-specific settings, session data)
.claude/settings.local.json
.cody/
.aider/
.continue/

# AI-generated reports (local only)
.reports/

# Instructor-only materials with answer keys (never publish)
instructor/
